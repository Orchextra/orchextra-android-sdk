<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

  <item
      android:id="@+id/navigation_scanner"
      android:icon="@drawable/ic_camera"
      android:title="@string/title_scanner"/>

  <item
      android:id="@+id/navigation_geofences"
      android:icon="@drawable/ic_geofences"
      android:title="@string/title_geofences"/>

  <item
      android:id="@+id/navigation_triggers_log"
      android:icon="@drawable/ic_triggers"
      android:title="@string/title_triggers_log"/>

</menu>
