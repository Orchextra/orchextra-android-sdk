apply plugin: 'com.android.library'
apply plugin: 'com.neenbedankt.android-apt'

def STRING = "String"
def BOOLEAN = "boolean"
def INT = "int"

buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'com.neenbedankt.gradle.plugins:android-apt:1.4'
  }
}

android {
  compileSdkVersion parent.ext.androidCompileSdkVersion
  buildToolsVersion parent.ext.androidBuildToolsVersion

  defaultConfig {
    minSdkVersion parent.ext.androidMinSdkVersion
    targetSdkVersion parent.ext.androidTargetSdkVersion
    versionCode parent.ext.androidVersionCode
    versionName parent.ext.androidVersionName

    buildConfigField STRING, "API_VERSION", '"v1"'
    buildConfigField STRING, "API_URL", '"http://api.s.orchextra.io"'
  }

  buildTypes {
    debug{
      testCoverageEnabled = true
      buildConfigField BOOLEAN, "RETROFIT_LOG", "false"
    }
    release {
      buildConfigField BOOLEAN, "RETROFIT_LOG", "false"

      minifyEnabled false
      proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
    }
  }

  testOptions {
    unitTests.returnDefaultValues = true
  }

  compileOptions {
    sourceCompatibility JavaVersion.VERSION_1_7
    targetCompatibility JavaVersion.VERSION_1_7
  }

}

dependencies {

  apt rootProject.ext.dependencies.daggerCompiler
  compile rootProject.ext.dependencies.dagger
  provided rootProject.ext.dependencies.javaXannotations

  compile project(':ggglib')
  compile project(':ggglogger')

  testCompile rootProject.ext.testDependencies.junit
  testCompile rootProject.ext.testDependencies.mockito

}
